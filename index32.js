const height = 10;
const base = 4;

console.log('the rectangular height is: 10, base is:4 and its perimiter is:',  height * 2 + base*2);
console.log('the rectangular height is: 10, base is:4 and its area is:', height*base);